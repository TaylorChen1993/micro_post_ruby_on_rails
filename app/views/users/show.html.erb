<% provide(:title, @user.name) %>
<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
            <h1>
                <%= gravatar_for @user %><!--在app/helpers/users_helper.rb中，但gravatar提供的所有头像都变成了一个-->
                <%= @user.name %>
            </h1>
        </section>
    </aside>
</div>