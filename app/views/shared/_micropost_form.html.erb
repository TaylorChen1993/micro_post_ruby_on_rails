<%= form_for(@micropost) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
<%= f.text_area :content, placeholder: "输入内容..." %>
</div>
<%= f.submit "发表", class: "btn btn-primary" %>
<% end %>